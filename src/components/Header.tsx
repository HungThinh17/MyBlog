import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <button id="menu-btn">☰</button>
      <h1>My Blog</h1>
    </header>
  );
};

export default Header;
